import React, { Fragment } from 'react';
import { Typography } from '@material-ui/core';

const About = () => {
    return (
        <Fragment>
            <Typography>
                About
            </Typography>
        </Fragment>
    )
}

export default About;